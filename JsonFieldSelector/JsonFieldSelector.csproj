<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="12.0.3" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Authors>Thiago Barradas</Authors>
    <Company>Thiago Barradas</Company>
    <Copyright>Ⓒ Thiago Barradas</Copyright>
    <PackageLicenseUrl>https://github.com/ThiagoBarradas/json-field-selector/blob/master/LICENSE</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/ThiagoBarradas/json-field-selector</PackageProjectUrl>
    <RepositoryUrl>https://github.com/ThiagoBarradas/json-field-selector</RepositoryUrl>
    <PackageTags>newtonsoft json field fields selector jtoken jobject jproperty</PackageTags>
    <PackageIconUrl>https://i.imgur.com/52HlaJN.png</PackageIconUrl>
    <Description>Select fields from a object, jtoken or a string (json), don't care if property is in depth objects or into an array.</Description>
  </PropertyGroup>
  
</Project>
